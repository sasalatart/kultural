<%= form_for(@event_type) do |f| %>
  <% if @event_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_type.errors.count, "error") %> prohibited this event_type from being saved:</h2>

      <ul>
      <% @event_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :examples %><br>
    <%= f.text_area :examples %>
  </div>
  <div class="field">
    <%= f.label :commentable_id %><br>
    <%= f.text_area :commentable_id %>
  </div>
  <div class="field">
    <%= f.label :commentable_type %><br>
    <%= f.text_area :commentable_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
