<%= form_for(current_user.memberships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :group_id, @group.id %></div>
  <%= f.submit "Join to group", class: ['button', 'button-def'] %>
<% end %>