<div class="events">

  <div class="center-align">
    <h1><%= @event.name %></h1>
    <div class="stats-container">
      <span id="avg-rating">
        <%= render partial: 'ratings/avg_rating', locals: { rateable: @event } %>
      </span>
      |
      <span id="num-reports">
        <%= render partial: 'reports/num_reports', locals: { reportable: @event } %>
      </span>
    </div>
  </div>

  <% if owner_of_event? %>
  <%= button_to 'Edit', edit_event_path(@event),
  method: 'get', class: 'button-def floated-left' %>
  <%= button_to 'Delete', event_path(@event),
  method: 'delete', class: 'button-del floated-left',
  data: { confirm: 'Are you sure?' } %>
  <% end %>

  <% if logged_in? %>
  <div class="floated-right">
    <%= render partial: 'reports/form', locals: { reportable: @event } %>
  </div>

  <div class="floated-right">
    <%= render partial: 'ratings/form', locals: { rateable: @event } %>
  </div>
  <% end %>

  <div class="big-container clear borders">
    <div class="info-container col-3">
      <%= image_tag 'events/default.png', height: 200 %>
    </div>
    <div class="info-container mini-map col-3-2">
      <div id="map"></div>
    </div>
  </div>

  <div class="big-container borders">
    <%= render 'event_info' %>
  </div>

  <div class="big-container borders">
    <% if logged_in? %>
    <div class="comment-input">
      <%= render partial: 'comments/form', locals: { commentable: @event } %>
    </div>
    <% end %>

    <div id="comments">
      <%= render partial: 'comments/commentable_comments', locals: { commentable: @event } %>
    </div>
  </div>
</div>

<script type="text/javascript">
  jQuery(document).ready(function(){
    handler = Gmaps.build('Google');
    handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
      markers = handler.addMarkers(<%= raw @hash.to_json %>);
      handler.bounds.extendWith(markers);
      handler.fitMapToBounds();
    });
  });
</script>

<!-- <script>
function initialize() {
  var mapOptions = {
    zoom: 17,
    center: new google.maps.LatLng(<%= @event.place.lat %>, <%= @event.place.lon %>)
  };

  var map = new google.maps.Map(document.getElementById('map'), mapOptions);
  var marker = new google.maps.Marker({
    position: new google.maps.LatLng(<%= @event.place.lat %>, <%= @event.place.lon %>),
    map: map,
    title: '<%= @event.place.name %>'
  });
  var mc = new MarkerClusterer(map, marker);
}

function loadScript() {
  var script = document.createElement('script');
  script.type = 'text/javascript';
  script.src = 'https://maps.googleapis.com/maps/api/js?v=3.exp' +
  '&callback=initialize';
  document.body.appendChild(script);
}

window.onload = loadScript;
google.maps.event.addDomListener(window, 'page:load', initialize);
</script>-->
