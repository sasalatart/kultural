<% url = @user.get_avatar(:thumb) %>
ajaxImages["<%= j ajax_avatar_user_path(@user) %>"] = "<%= j image_tag url %>"
$('[data-image-ajax-url="<%= ajax_avatar_user_path(@user) %>"]').replaceWith("<%= j image_tag url %>");
