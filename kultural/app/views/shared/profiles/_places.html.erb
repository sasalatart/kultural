<% # Assumes owner as parameter %>
<% if owner.places.count > 0%>
<div class="borders normal-container">
  <h3>My places</h3>
    <% owner.places.each do |place| %>
      <div><%= link_to place.name, place %></div>
    <% end %>
</div>
<% end %>